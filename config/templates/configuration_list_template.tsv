# Configuration List (TSV)
#
# Each line: <base-config> <stage-config> [<stage-config> ...]
# Paths are relative to this TSV file.
# The base config must define test_set_id under the top-level or [run].
#
# Examples:
#
# Align -> Correct -> Collapse
# config/templates/inputs.toml	config/templates/align.toml	config/templates/correct.toml	config/templates/collapse.toml
#
# Align -> Regionalize -> Correct -> Collapse
# config/templates/inputs.toml	config/templates/align.toml	config/templates/regionalize.toml	config/templates/correct.toml	config/templates/collapse.toml
#
# Align -> Transcriptome (FLAIR >= 3)
# config/templates/inputs.toml	config/templates/align.toml	config/templates/transcriptome.toml
#
# Align -> Regionalize -> Transcriptome (FLAIR >= 3)
# config/templates/inputs.toml	config/templates/align.toml	config/templates/regionalize.toml	config/templates/transcriptome.toml
#
# Optional downstream stages:
#  - Add config/templates/combine.toml after collapse/transcriptome
#  - Add config/templates/quantify.toml after collapse/transcriptome/combine
